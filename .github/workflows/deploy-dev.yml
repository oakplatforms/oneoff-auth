name: Deploy to Dev

on:
  push:
    branches: [ dev ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    env:
      SLS_DISABLE_DASHBOARD: "true"
      SLS_TELEMETRY_DISABLED: "true"
      SLS_ORG: ""
      SLS_APP: ""
    steps:
    - uses: actions/checkout@v4

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'
        cache: 'npm'

    - name: Install dependencies
      run: npm ci

    - name: Setup AWS credentials
      uses: aws-actions/configure-aws-credentials@v4
      with:
        aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
        aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        aws-region: us-east-1

    - name: Install Serverless Framework
      run: npm install -g serverless

    - name: Configure Serverless Framework
      run: |
        mkdir -p ~/.serverless
        echo '{"frameworkVersion": "3", "telemetryDisabled": true, "dashboard": {"enabled": false}}' > ~/.serverless/config.json

    - name: Deploy to Dev
      run: serverless deploy --stage dev --verbose
      env:
        CONSUMER_USER_POOL_ID: ${{ secrets.CONSUMER_USER_POOL_ID }}
        CONSUMER_CLIENT_ID: ${{ secrets.CONSUMER_CLIENT_ID }}
        CONSUMER_DEFAULT_USERNAME: ${{ secrets.CONSUMER_DEFAULT_USERNAME }}
        CONSUMER_DEFAULT_PASSWORD: ${{ secrets.CONSUMER_DEFAULT_PASSWORD }}
        API_BASE_URL: ${{ vars.API_BASE_URL }}
